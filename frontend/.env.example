# API base URL (leave as /api when using Vite proxy in dev)
VITE_API_URL=/api

# WebSocket server (leave empty to use same host)
VITE_WS_URL=http://localhost:4000

# Optional: App name shown in metadata
VITE_APP_NAME=StudyFlow
